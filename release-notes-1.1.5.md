## Nexora v1.1.5

### 通知中心

- 新增「通知」导航分组，包含**公告**、**联系管理员**、**版本更新**三个页面。
- 公告：对接后端公告 API，支持列表与详情查看。
- 联系管理员：对接聊天 API 与 WebSocket，支持实时收发消息、管理员消息 Toast 提醒。
- 版本更新：对接后端 `/updates/latest` 与 `VERSION_UPDATE` 推送，支持自动检查、自动下载、SHA-256 校验与一键安装。

### 个人资料

- 左下角登录区域新增**编辑资料**按钮，可修改昵称与头像。
- 左下角展示用户头像与昵称；有头像时优先显示头像，不再使用默认图标。
- 昵称与头像分别调用独立接口，可单独修改其中一项。

### 版本更新改进

- 优先按 `versionName` 比较版本，兼容管理端不同的 `versionCode` 编码。
- 修复下载后共享 `HttpClient` 被释放导致无法再次检查更新的问题。
- 修复下载完成后文件句柄未关闭导致 SHA-256 校验失败的问题。
- 已下载安装包状态持久化，重启应用后不会重复自动下载。

### 设置

- 新增「自动下载新版本」开关（默认开启）。

### 其他

- 移除系统菜单中的 GitHub「检查更新」页面，统一通过「通知 → 版本更新」获取管理员发布的版本。

### 安装包

- Windows x64：`Nexora-Setup-1.1.5.exe`
