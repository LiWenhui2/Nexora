## Nexora v1.1.4

### 修复：登录后订阅更新超时 / 开机节点列表为空

- **绕过系统代理**：认证 API 与订阅拉取改用直连 HTTP，不再受 Windows 系统代理（`127.0.0.1:10809`）影响；避免 Core 未启动时请求全部超时。
- **启动清理残留代理**：Core 未运行时，自动清除指向本机代理端口的残留系统代理设置。
- **网络超时保留本地节点**：云端同步或订阅刷新遇到超时/网络错误时，不再误删本地节点。
- **同步前恢复会话**：更新订阅前先尝试刷新 access token，避免界面显示已登录但 token 已过期导致同步失败。

### v1.1.3 起累计改进（本包已包含）

- 登录状态校验 access token 过期；会话 token DPAPI 加密存储；refresh token 刷新互斥锁。
- 后台每 30 分钟自动刷新 token；401 时支持强制刷新。
- 云端订阅始终从 API 拉取最新列表，并清理已删除的本地订阅地址。

### 安装包

- Windows x64：`Nexora-Setup-1.1.4.exe`
