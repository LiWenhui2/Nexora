# Nexora 1.1.15

## TUN 模式修复

- 修复 TUN 虚拟网卡已创建、路由已接管，但 DNS 请求超时导致系统无法访问域名的问题。
- 增加 sing-box DNS 上游、DNS 劫持规则和默认域名解析器，使 Windows DNS 请求能够通过 TUN 正常解析。
- TUN 出站统一转发到本地 SOCKS5 端口，并排除当前代理节点地址，避免代理流量重新进入 TUN 形成路由回环。
- 启动 TUN 前自动调用 sing-box 检查生成的配置，配置无效时直接显示具体错误。
- 捕获 sing-box 标准输出、错误输出及异常退出信息，便于在应用日志中定位运行时故障。
- 修复请求管理员权限重启时，新进程可能被单实例检查提前关闭的问题。

## 验证

- 使用项目随附的 sing-box 1.13.13 完成配置检查。
- 通过 .NET Release 构建与 Windows x64 自包含发布验证。
