## Nexora v1.1.6

### 设备管理与强制下线

- 收到 WebSocket `FORCE_LOGOUT` 时，在应用窗口内显示自定义下线详情弹窗（非系统弹窗）。
- 弹窗展示下线原因、设备上限、本会话 ID，以及新登录设备的会话、客户端类型、设备名称、系统、IP、地理位置等信息。
- 空字段统一显示为「未知」；确认后跳转登录页。

### 账号安全

- 编辑资料页新增「重置密码」入口，支持邮箱验证码重置登录密码。
- 登录页新增「忘记密码」流程，弹窗支持滚动查看完整表单。

### 认证与设备信息

- 登录与 Token 刷新时携带设备信息（`clientType`、`deviceId`、`deviceName`、系统与应用版本等）。
- 收到 `USER_PROFILE_UPDATED` 或重新上线时自动拉取并保存用户资料。

### 联系管理员

- 聊天页展示管理员默认头像与用户昵称。
- 优化小屏笔记本下的联系管理员页布局，避免输入框被遮挡。
- 未登录状态文案调整为「登录账号后可与管理员实时沟通」。

### 其他

- 登录/注册成功且订阅解析成功后，自动开启代理。
- 公告页未登录时展示引导登录面板。

### 安装包

- Windows x64：`Nexora-Setup-1.1.6.exe`
