<Window x:Class="NaiwaProxy.Dialogs.ForceLogoutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nexora"
        SizeToContent="WidthAndHeight"
        MinWidth="420"
        MaxWidth="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        FontSize="14">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryButtonForegroundBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{DynamicResource AccentGradientBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimaryBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Margin="20"
            Background="{DynamicResource PanelBrush}"
            BorderBrush="{DynamicResource LineBrush}"
            BorderThickness="1"
            CornerRadius="16"
            Padding="22"
            SnapsToDevicePixels="True">
        <Grid MinWidth="360">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                <Border Width="44"
                        Height="44"
                        CornerRadius="12"
                        BorderThickness="1"
                        Background="{DynamicResource ErrorIconBackgroundBrush}"
                        BorderBrush="{DynamicResource ErrorIconBorderBrush}"
                        Margin="0,0,14,0">
                    <TextBlock Text="!"
                              FontSize="22"
                              FontWeight="Bold"
                              Foreground="{DynamicResource ErrorIconBrush}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </Border>
                <TextBlock x:Name="HeadlineText"
                           Text="账号已下线"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="DescriptionText"
                       Grid.Row="1"
                       Foreground="{DynamicResource MutedBrush}"
                       TextWrapping="Wrap"
                       LineHeight="22"
                       Margin="0,0,0,16"/>

            <Border x:Name="DetailsPanel"
                    Grid.Row="2"
                    Background="{DynamicResource Panel2Brush}"
                    BorderBrush="{DynamicResource LineBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="14"
                    Margin="0,0,0,18"
                    Visibility="Collapsed">
                <ItemsControl x:Name="DetailsItems">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       FontSize="14"
                                       Margin="0,0,0,6"
                                       TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

            <Button x:Name="ConfirmButton"
                    Grid.Row="3"
                    Content="我知道了，重新登录"
                    Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Stretch"
                    IsDefault="True"
                    Click="ConfirmButton_Click"/>
        </Grid>
    </Border>
</Window>
